import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["elements-resized.envatousercontent.com", "images.unsplash.com"], // Add more here
  },
};

export default nextConfig;
