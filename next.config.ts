const nextConfig = {
  images: {
    domains: ["elements-resized.envatousercontent.com", "images.unsplash.com"],
  },
  typescript: { ignoreBuildErrors: true }, // ← remove once fixed
};

export default nextConfig;
